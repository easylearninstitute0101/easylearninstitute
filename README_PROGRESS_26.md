# Progress 26 — PWA + Mobile UI + Offline/Loading/Error States

- PWA manifest and install prompt retained.
- Service worker cache version bumped and restricted to same-origin app shell/assets; Supabase/API responses are never cached.
- Offline/online status banner added.
- Global module loading overlay added for navigation.
- Global unhandled-error and unhandled-promise-rejection UI added.
- Mobile controls use touch-friendly minimum heights and 16px form text to reduce mobile browser zoom.
- Existing responsive layouts and role/tenant/security behavior retained.
